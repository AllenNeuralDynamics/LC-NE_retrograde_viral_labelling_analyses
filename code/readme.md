### Environment

Install `vta` as a local package.

```
cd code
pip install -e .
```

### Copilot
 - The copilot file is at `/code/.vscode/GitHub.copilot-1.224.1077.vsix`. 
 - Right click, and select `Install extension vsix` to install the extension
 - Log in through the gui to get copilot code completion.


### Notes

 - [Tutorial for ccf interaction](https://allensdk.readthedocs.io/en/latest/_static/examples/nb/reference_space.html)